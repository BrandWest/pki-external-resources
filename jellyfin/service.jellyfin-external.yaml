# Jellyfin Service (headless, routes to external IP via Endpoints)
apiVersion: v1
kind: Service
metadata:
  name: jellyfin-external
  namespace: external-resources
spec:
  ports:
  - port: 8096
    targetPort: 8096
    protocol: TCP
    name: http
  - port: 30013
    targetPort: 30013
    protocol: TCP
    name: gui
---
# Endpoints pointing to TrueNAS jellyfin
apiVersion: v1
kind: Endpoints
metadata:
  name: jellyfin-external
  namespace: external-resources
  annotations:
    avp.kubernetes.io/path: "secrets/data/infra/truenas/prod"
subsets:
- addresses:
  - ip: <truenas_ip>
  ports:
  - port: 8096
    name: http
  - port: 30013
    name: gui
